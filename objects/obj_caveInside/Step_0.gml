/// @description Insert description here
// You can write your code in this editor
depth=-1*(bbox_top);
if(state==0 && place_meeting(x,y,obj_hero)){
	with(obj_priestess){
		if(state==10) state=23;	
	}
	state=1;
}